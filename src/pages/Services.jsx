import {Link} from 'react-router-dom';export default function Services({services}){return <section className="section page"><span className="eyebrow">SERVIÇOS</span><h1>Serviços</h1><div className="cards">{services.filter(s=>s.active).map(s=><article className="card" key={s.id}><span>{s.category}</span><h3>{s.name}</h3><p>{s.description}</p><div className="row"><b>R$ {s.price.toFixed(2).replace('.',',')}</b><small>{s.duration} min</small></div><Link className="btn full" to={`/agendar?service=${s.id}`}>Agendar</Link></article>)}</div></section>}
