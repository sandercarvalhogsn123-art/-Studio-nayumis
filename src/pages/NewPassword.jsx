import {useState} from 'react';
import {Link,useNavigate} from 'react-router-dom';
import {supabase} from '../lib/supabase';
export default function NewPassword(){const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [busy,setBusy]=useState(false);const nav=useNavigate();async function save(e){e.preventDefault();if(password.length<8)return alert('Use pelo menos 8 caracteres.');if(password!==confirm)return alert('As senhas não coincidem.');setBusy(true);const {error}=await supabase.auth.updateUser({password});setBusy(false);if(error)return alert(error.message);alert('Senha atualizada com sucesso.');nav('/admin/login')}return <section className="login-page"><form className="login-card" onSubmit={save}><div className="logo large"><span>NS</span></div><h1>Definir nova senha</h1><p>Crie uma nova senha para o painel administrativo.</p><label>Nova senha<input type="password" minLength="8" value={password} onChange={e=>setPassword(e.target.value)} required/></label><label>Confirmar senha<input type="password" minLength="8" value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label><button className="btn full" disabled={busy}>{busy?'Salvando...':'Salvar nova senha'}</button><Link to="/admin/login">Voltar ao login</Link></form></section>}
