export default function Professionals({professionals,settings}){const c=settings?.content||{};return <section className="section page"><span className="eyebrow">{c.professionalsEyebrow||'EQUIPE'}</span><h1>{c.professionalsTitle||'Profissionais'}</h1><div className="cards">{professionals.filter(p=>p.active).map(p=><article className="card" key={p.id}>{p.image?<div className="avatar big"><img src={p.image} alt={p.name}/></div>:<div className="avatar big">{p.name.split(' ').map(x=>x[0]).slice(0,2).join('')}</div>}<h3>{p.name}</h3><p>{p.bio}</p></article>)}</div></section>}
