import{useState}from'react';import{supabase}from'../../lib/supabase';
export default function ServicesAdmin({data,setData}){const blank={name:'',category:'Nails',description:'',price:0,duration:60,image:'',active:true};const[f,setF]=useState(blank);const[saving,setSaving]=useState(false);const[error,setError]=useState('');const[editingImage,setEditingImage]=useState(null);const[imageUrl,setImageUrl]=useState('');
function readImage(file,cb){if(!file)return;const r=new FileReader();r.onload=()=>cb(r.result);r.readAsDataURL(file)}
async function add(e){e.preventDefault();if(!f.name.trim())return;setSaving(true);setError('');const payload={name:f.name.trim(),category:f.category.trim()||'Outros',description:f.description.trim(),price:Number(f.price||0),duration_minutes:Number(f.duration||60),image_url:f.image||null,active:true,sort_order:data.services.length+1};const{data:created,error:dbError}=await supabase.from('services').insert(payload).select().single();setSaving(false);if(dbError)return setError(dbError.message);setData(d=>({...d,services:[...d.services,{id:created.id,name:created.name,category:created.category||'',description:created.description||'',price:Number(created.price||0),duration:created.duration_minutes||0,active:created.active!==false,image:created.image_url||''}]}));setF(blank)}
async function saveImage(service){setSaving(true);setError('');const{error:dbError}=await supabase.from('services').update({image_url:imageUrl||null}).eq('id',service.id);setSaving(false);if(dbError)return setError(dbError.message);setData(d=>({...d,services:d.services.map(s=>s.id===service.id?{...s,image:imageUrl}:s)}));setEditingImage(null);setImageUrl('')}
async function del(id){if(!confirm('Excluir serviço?'))return;const{error:dbError}=await supabase.from('services').delete().eq('id',id);if(dbError)return setError(dbError.message);setData(d=>({...d,services:d.services.filter(s=>s.id!==id)}))}
return <><div className="admin-head"><div><span className="eyebrow">CATÁLOGO COMPLETO</span><h1>Serviços</h1><p>Você pode adicionar quantos serviços quiser, inclusive vários estilos de Lash.</p></div></div><form className="panel form-grid" onSubmit={add}><label>Nome<input placeholder="Ex.: Volume brasileiro" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Categoria<select value={f.category} onChange={e=>setF({...f,category:e.target.value})}><option>Nails</option><option>Lash</option><option>Sobrancelhas</option><option>Outros</option></select></label><label>Preço<input type="number" step="0.01" value={f.price} onChange={e=>setF({...f,price:e.target.value})}/></label><label>Duração (min)<input type="number" value={f.duration} onChange={e=>setF({...f,duration:e.target.value})}/></label><label className="span2">Descrição<input placeholder="Descrição do serviço" value={f.description} onChange={e=>setF({...f,description:e.target.value})}/></label><label className="span2">Foto do serviço<input type="file" accept="image/*" onChange={e=>readImage(e.target.files?.[0],img=>setF({...f,image:img}))}/></label>{f.image&&<div className="span2 photo-preview"><img src={f.image} alt="Prévia do serviço"/><button className="text-button" type="button" onClick={()=>setF({...f,image:''})}>Remover foto</button></div>}<button className="btn" disabled={saving}>{saving?'Salvando...':'Adicionar serviço'}</button></form>{error&&<div className="panel"><small>{error}</small></div>}<div className="panel">{data.services.map(s=><div className="service-admin-row" key={s.id}>{s.image?<img className="service-admin-thumb" src={s.image} alt={s.name}/>:<div className="service-admin-thumb empty">Sem foto</div>}<div className="service-admin-info"><b>{s.name}</b><small>{s.category} • {s.duration} min • R$ {Number(s.price).toFixed(2)}</small>{editingImage===s.id?<div className="image-edit"><label>Escolher da galeria<input type="file" accept="image/*" onChange={e=>readImage(e.target.files?.[0],setImageUrl)}/></label><input placeholder="ou cole um link da foto" value={imageUrl} onChange={e=>setImageUrl(e.target.value)}/>{imageUrl&&<img className="service-admin-thumb" src={imageUrl} alt="Prévia"/>}<button className="btn" type="button" disabled={saving} onClick={()=>saveImage(s)}>Salvar foto</button><button className="text-button" type="button" onClick={()=>setEditingImage(null)}>Cancelar</button></div>:<button className="text-button" type="button" onClick={()=>{setEditingImage(s.id);setImageUrl(s.image||'')}}>{s.image?'Trocar foto':'Adicionar foto'}</button>}</div><button className="danger" onClick={()=>del(s.id)}>Excluir</button></div>)}</div></>}
