import {useState} from 'react';export default function ServicesAdmin({data,setData}){const blank={name:'',category:'Nails',description:'',price:0,duration:60,active:true};const [f,setF]=useState(blank);function add(e){e.preventDefault();setData(d=>({...d,services:[...d.services,{...f,id:crypto.randomUUID(),price:Number(f.price),duration:Number(f.duration)}]}));setF(blank)}function del(id){if(confirm('Excluir serviço?'))setData(d=>({...d,services:d.services.filter(s=>s.id!==id)}))}return <><div className="admin-head"><div><span className="eyebrow">CATÁLOGO</span><h1>Serviços</h1></div></div><form className="panel form-grid" onSubmit={add}><input placeholder="Nome" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><input placeholder="Categoria" value={f.category} onChange={e=>setF({...f,category:e.target.value})}/><input type="number" placeholder="Preço" value={f.price} onChange={e=>setF({...f,price:e.target.value})}/><input type="number" placeholder="Duração (min)" value={f.duration} onChange={e=>setF({...f,duration:e.target.value})}/><input className="span2" placeholder="Descrição" value={f.description} onChange={e=>setF({...f,description:e.target.value})}/><button className="btn">Adicionar serviço</button></form><div className="panel">{data.services.map(s=><div className="list-row" key={s.id}><div><b>{s.name}</b><small>{s.duration} min • R$ {s.price}</small></div><button className="danger" onClick={()=>del(s.id)}>Excluir</button></div>)}</div></>}
