import {useState} from 'react';export default function SiteEditor({data,setData}){const [s,setS]=useState(data.settings);function file(e){const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>setS({...s,logo:r.result});r.readAsDataURL(f)}function save(){setData(d=>({...d,settings:s}));alert('Alterações salvas.')}return <><div className="admin-head"><h1>Editar site</h1></div><div className="panel form-grid"><label>Nome<input value={s.name} onChange={e=>setS({...s,name:e.target.value})}/></label><label>Subtítulo<input value={s.subtitle} onChange={e=>setS({...s,subtitle:e.target.value})}/></label><label>WhatsApp<input value={s.phone} onChange={e=>setS({...s,phone:e.target.value})}/></label><label>Instagram<input value={s.instagram} onChange={e=>setS({...s,instagram:e.target.value})}/></label><label className="span2">Endereço<input value={s.address} onChange={e=>setS({...s,address:e.target.value})}/></label><label>Cor da capa<input type="color" value={s.hero} onChange={e=>setS({...s,hero:e.target.value})}/></label><label>Foto de perfil/logo<input type="file" accept="image/*" onChange={file}/></label>{s.logo&&<div className="crop-preview span2"><img src={s.logo}/><p>Prévia. A versão final pode usar um componente de crop/zoom com react-easy-crop.</p></div>}<button className="btn" onClick={save}>Salvar alterações</button></div></>}
