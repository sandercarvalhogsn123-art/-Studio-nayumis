import {useState} from 'react';import {supabase} from '../lib/supabase';export default function Recover(){const [email,setEmail]=useState('sandercarvalhogsn123@gmail.com');const [busy,setBusy]=useState(false);async function go(e){e.preventDefault();setBusy(true);const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:location.origin+'/admin/nova-senha'});setBusy(false);alert(error?error.message:'Link de recuperação enviado. Verifique seu e-mail.')}return <section className="login-page"><form className="login-card" onSubmit={go}><h1>Recuperar senha</h1><p>Informe seu e-mail de administrador.</p><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button className="btn full" disabled={busy}>{busy?'Enviando...':'Enviar link'}</button></form></section>}
